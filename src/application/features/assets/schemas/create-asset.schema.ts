import { z } from 'zod';

/**
 * CreateAsset validation schema — equivalent to Application.Features.Assets.Validators.CreateAssetValidator.
 * Intentionally empty — stub, like the .NET origin.
 * Add fields and validation rules as the feature is implemented.
 */
export const createAssetSchema = z.object({
  // Add fields here when implementing the feature
});
